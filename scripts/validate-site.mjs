import fs from "node:fs";import path from "node:path";import{canonicalUrl,LOCALES,PAGES}from"./site-config.mjs";const root=process.cwd(),errors=[];for(const locale of Object.keys(LOCALES)){for(const page of PAGES){const file=path.join(root,locale,page.route,"index.html");if(!fs.existsSync(file)){errors.push(`Missing ${file}`);continue;}const html=fs.readFileSync(file,"utf8");if(/{{[^}]+}}/.test(html))errors.push(`Unresolved token in ${file}`);if(!html.includes(canonicalUrl(locale,page.route)))errors.push(`Missing canonical in ${file}`);}}if(errors.length){console.error(errors.join("\n"));process.exit(1);}console.log(`Site validation passed: ${PAGES.length} route(s), ${Object.keys(LOCALES).length} locale(s).`);
